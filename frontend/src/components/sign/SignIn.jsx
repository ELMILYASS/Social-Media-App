import React from "react";

function SignIn({ position }) {
  return (
    <div
      className={`absolute transform  w-full translate-x-[${position}] `}
    >
      SignIn
    </div>
  );
}

export default SignIn;
