import React from "react";

function SignUp({ position }) {
  return (
    <div
      className={`absolute transform w-full duration-[0.3s] translate-x-[${position}] `}
    >
      SignUp
    </div>
  );
}

export default SignUp;
